import { defineConfig, devices } from "@playwright/test";

/**
 * E2E config. Boots the Next dev server and runs read-only UI smoke tests.
 * Wallet/on-chain flows are covered by the Anvil integration test + unit tests;
 * these specs verify the rendered app shell and client-side navigation.
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: "list",
  use: {
    baseURL: "http://127.0.0.1:3100",
    trace: "on-first-retry",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "corepack pnpm dev --port 3100",
    url: "http://127.0.0.1:3100",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
